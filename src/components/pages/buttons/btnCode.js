const btnCode = `
  // Import the button component
  import { Button } from 'react-bootstrap';

  // Define a component
  const MyButton = () => {
    return <Button>Click me!</Button>;
  };

  // Set the Bootstrap theme for the button
  const SuccessButton = () => {
    return <Button variant="success">Click me!</Button>;
  };  
`;

export default btnCode;
