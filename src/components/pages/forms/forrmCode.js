const formCode = `
// import the components for the form
import { Form, Button } from 'react-bootstrap';

// define the component
const ExampleForm = () => {
  return (
    <Form>
      {/* Additional code here */}
    </Form>
 );
`;

export default formCode;
